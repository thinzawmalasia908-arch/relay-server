# WebSocket Relay Server

Camera + Screen streaming relay server with Turso DB logging.

## Deploy on Koyeb

- Run command: `npm start`
- Port: `8000`
- Environment variables: `TURSO_DATABASE_URL`, `TURSO_AUTH_TOKEN`